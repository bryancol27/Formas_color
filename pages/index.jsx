import React from 'react';

// Import components
import { Navbar } from '@components/navbar';

const Home = () => {
    return (
        <div>
            <Navbar/>
            at Home
        </div>
    );
};

export default Home;