import React from 'react';

// Import templates
import { Header } from '@templates/Header';

const Home = () => {
    return (
        <React.Fragment>
            <Header/>
            
        </React.Fragment>
    );
};

export default Home;